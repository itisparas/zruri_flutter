import 'package:flutter/material.dart';

class PostAdPage extends StatelessWidget {
  const PostAdPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: SafeArea(
        child: Text('Post ads page.'),
      ),
    );
  }
}
